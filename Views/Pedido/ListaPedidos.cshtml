@{
    ViewData["Title"] = "Pagina de pedidos";
}


@model List<EntidadesSistema.PedidoViewModel>

<div class="text-center">
    <h2 class="display-4">Listado de Pedidos</h2>
</div>
<div class="text-center">
    <table class="table table-striped" style="text-align:center">
    @if (Model.Count() != 0)
    {
        <tr>
            <th>Numero</th> 
            <th>Direccion</th>
            <th>Obersvacion</th>
            <th>Destino</th>
            <th>Estado</th>
            <th>Acción</th>
        </tr>
        @foreach (var item in Model)
        {
            @if(@item.Estado == "Aceptado")
            {
                <tr>
                    <td>@item.Numero</td>
                    <td>@item.ClienteID</td>
                    <td>@item.Observacion</td>
                    <td>@item.ClienteID</td>
                    <td>@item.Estado</td>
                    <td>
                        <a button type="button" class="btn btn-secondary btn-sm" href="AceptarPedido?_Id=@item.Numero">Aceptar</a>
                    </td>
                </tr>
            }
        }
    }
    </table>

</div>