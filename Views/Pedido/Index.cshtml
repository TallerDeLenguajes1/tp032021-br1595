@{
    ViewData["Title"] = "Pagina de pedidos";
}


@model List<EntidadesSistema.Pedido>

<div class="text-center">
    <h2 class="display-4">Listado de Pedidos</h2>
</div>
<a type="button" asp-action="AltaPedido" class="btn btn-primary">Dar Alta Pedido</a>

<div class="text-center">
    <table class="table table-striped" style="text-align:center">
    @if (Model.Count() != 0)
    {
        <tr>
            <th>Numero</th> 
            <th>Direccion</th>
            <th>Obersvacion</th>
            <th>Destino</th>
            <th>Estado</th>
        </tr>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.Numero</td>
                <td>@item.ClienteID</td>
                <td>@item.Observacion</td>
                <td>@item.ClienteID</td>
                <td>@item.Estado</td>
            </tr>
        }
    }
    </table>

</div>